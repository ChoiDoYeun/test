# run.py

from motor.move_position import move_position_back
import time

def run():
    print("Run 시작")
    # move_position_back(225,350,50)
    # 원점으로 이동
    # move_zero_point (x)
    # move_zero_point (z)
    # move_zero_point (y)
    time.sleep(0.5)
